import animateBurger from 'src/components/burger-menu/burger';

describe('test burger menu', () => {
    it('clicked active', () => {
        animateBurger();

        // expect(animateBurger()).
    });
});
