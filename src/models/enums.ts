export enum DomClassManipulation {
    add = 'add',
    remove = 'remove',
    toggle = 'toggle',
}

export enum ThemeName {
    dark = 'Dark',
    light = 'Light',
}

export enum SelectedLanguage {
    english = 'ENG',
    russian = 'RU',
}
