export enum DomClassManipulation {
    add = 'add',
    remove = 'remove',
    toggle = 'toggle',
}

export enum ThemeName {
    dark = 'Dark',
    light = 'Light',
}

export enum SelectedLangugage {
    english = 'ENG',
    russian = 'RU',
}
